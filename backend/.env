# Ruta donde se guardarán los embeddings (persistente)
CHROMA_PATH=../chroma

# Límite de tokens antes de cambiar de modelo
MAX_TOKENS=7500

# Lista de modelos en orden de preferencia (separados por coma)
MODELS=llama3:70b,mistral:7b,phi:2
